/** Shared unit constants of the simulation and the renderer. */

/** Seconds a corpse stays on the battlefield before its entity is freed (it sinks during the last 4 s). */
export const CORPSE_SECONDS = 22;

/** Fraction of the swing animation at which the blow lands: swing duration = windup / IMPACT_FRACTION. */
export const IMPACT_FRACTION = 0.45;

/** Seconds of the red flash after a hit. */
export const HIT_FLASH_SECONDS = 0.18;
